public class BasicOperator {
    public static void main(String[] args) {
        int x = 10;
        int y = 5;
        int z = 2;
        int hasil = x + y;

        // (+=) ->  z = z + 4
        // (++) -> z = z + 1
        // (--) -> z = z - 1
        // (*=) -> z = z * 2
        System.out.println(z*=2);

    }
}
